@model IEnumerable<AppWeb.ViewModels.TypeColisViewModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomTypeColis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomTypeColis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeColisid }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeColisid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeColisid })
        </td>
    </tr>
}

</table>
