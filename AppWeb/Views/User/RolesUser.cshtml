@model IEnumerable<AppWeb.ViewModels.RoleViewModels>
@{

    AppWeb.ViewModels.UserViewModel _us = (AppWeb.ViewModels.UserViewModel)ViewBag.user;

}
<p>
 
</p>
<h3>Roles de @ViewBag.nomuser</h3>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            @Html.ActionLink("Supprimer", "DeleteUserRoles", new { id1=_us.Userid,id2=item.Nom})
        </td>
    </tr>
}

</table>
