@model IEnumerable<AppWeb.ViewModels.NatureColisViewModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomNatureColis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomNatureColis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NatureColisid }) |
            @Html.ActionLink("Details", "Details", new { id=item.NatureColisid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NatureColisid })
        </td>
    </tr>
}

</table>
